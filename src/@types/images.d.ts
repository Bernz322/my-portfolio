declare module "*.png";
declare module "*.svg" {
  const content: any;
  export default content;
}

// Change projects theme depending on the current page theme, update data.ts
// When inside a single page project, when a nav link btn is clicked, it will go back to the main page and not go to
// particular section
// Add more projects but in a simpler manner
// Add more details to a project - map tags out
// Animations
// Domain
